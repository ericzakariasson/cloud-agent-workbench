# Retro Minesweeper

Tiny no-build retro Minesweeper made with plain HTML/CSS/JS.

## Run locally

1. From this repo root, start a static server:
   - `python3 -m http.server 8000`
2. Open `http://localhost:8000` in a browser.
3. Left click to reveal cells, right click to place flags.
